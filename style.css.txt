* { margin:0; padding:0; box-sizing:border-box; }

:root{
  --bg:#0a0a0a;
  --fg:#d4ff2a;   /* neon lime */
}

@font-face{
  font-family:'Mono';
  src:local('JetBrains Mono'), local('Courier New');
}

body{
  background:var(--bg);
  color:var(--fg);
  font-family:'JetBrains Mono','Courier New',monospace;
  min-height:100vh;
  overflow-x:hidden;
  position:relative;
  background-image:
    radial-gradient(circle at 20% 30%, rgba(212,255,42,0.04) 0%, transparent 50%),
    radial-gradient(circle at 80% 70%, rgba(212,255,42,0.03) 0%, transparent 50%);
}

body::before{
  content:"";
  position:fixed; inset:0;
  background-image:
    repeating-linear-gradient(0deg, transparent 0, transparent 2px, rgba(212,255,42,0.02) 3px);
  pointer-events:none;
  z-index:1;
}

/* CORNERS */
.corner{
  position:fixed;
  width:18px; height:18px;
  background:var(--fg);
  z-index:10;
}
.tl{top:20px; left:20px;}
.tr{top:20px; right:20px;}
.bl{bottom:20px; left:20px;}
.br{bottom:20px; right:20px;}

/* SIDE TEXT */
.side-text{
  position:fixed;
  top:80px;
  font-size:10px;
  letter-spacing:1px;
  color:var(--fg);
  line-height:1.6;
  z-index:10;
  text-transform:lowercase;
}
.side-text.left{ left:22px; }
.side-text.right{ right:22px; }

.index-tag{
  position:fixed;
  bottom:60px;
  font-size:12px;
  font-weight:bold;
  z-index:10;
}
.index-tag.left{ left:20px; }
.index-tag.right{ right:20px; }

/* HERO */
.hero{
  min-height:100vh;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  position:relative;
  padding:0 100px;
}

.crosshair{
  position:absolute;
  top:35%;
  left:50%;
  transform:translateX(-50%);
  width:40px; height:40px;
  border:1px solid var(--fg);
  opacity:0.4;
}
.crosshair::before, .crosshair::after{
  content:"";
  position:absolute;
  background:var(--fg);
}
.crosshair::before{ top:50%; left:-20px; width:80px; height:1px; }
.crosshair::after{ left:50%; top:-20px; height:80px; width:1px; }

.glitch{
  font-size:clamp(3rem, 10vw, 8rem);
  font-weight:900;
  letter-spacing:0.15em;
  color:var(--fg);
  text-shadow:
    2px 0 rgba(255,0,80,0.5),
    -2px 0 rgba(0,200,255,0.4);
  animation:flicker 3s infinite;
  position:relative;
}

@keyframes flicker{
  0%,100%{opacity:1;}
  92%{opacity:1;}
  93%{opacity:0.4;}
  94%{opacity:1;}
  96%{opacity:0.7;}
  97%{opacity:1;}
}

.subtitle{
  margin-top:20px;
  font-size:12px;
  letter-spacing:4px;
  opacity:0.7;
}

/* FEED */
.feed-section{
  padding:100px 100px;
  position:relative;
  z-index:5;
}

.section-header{
  display:flex;
  align-items:center;
  gap:20px;
  margin-bottom:60px;
  border-bottom:1px solid rgba(212,255,42,0.2);
  padding-bottom:20px;
}
.bar{
  width:4px; height:24px;
  background:var(--fg);
}
.section-header h2{
  font-size:14px;
  letter-spacing:3px;
  font-weight:700;
}
.status{
  margin-left:auto;
  font-size:11px;
  opacity:0.6;
  padding:4px 10px;
  border:1px solid var(--fg);
}

.feed-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill, minmax(320px,1fr));
  gap:20px;
}

.tweet-card{
  background:rgba(212,255,42,0.03);
  border:1px solid rgba(212,255,42,0.3);
  padding:20px;
  position:relative;
  transition:.3s;
  cursor:pointer;
  overflow:hidden;
}
.tweet-card:hover{
  background:rgba(212,255,42,0.08);
  border-color:var(--fg);
  transform:translateY(-3px);
}
.tweet-card::before{
  content:"";
  position:absolute;
  top:8px; right:8px;
  width:8px; height:8px;
  background:var(--fg);
}

.tweet-date{
  font-size:10px;
  opacity:0.5;
  letter-spacing:2px;
  margin-bottom:10px;
}
.tweet-text{
  font-size:14px;
  line-height:1.6;
  color:#e6ffb0;
  word-wrap:break-word;
}
.tweet-media{
  margin-top:15px;
  width:100%;
  border:1px solid rgba(212,255,42,0.2);
  filter:contrast(1.1) saturate(0.9);
}

.error, .loading{
  grid-column:1/-1;
  text-align:center;
  padding:40px;
  opacity:0.5;
  font-size:12px;
  letter-spacing:2px;
}

/* FOOTER */
footer{
  padding:40px 100px;
  text-align:center;
  position:relative;
  z-index:5;
}
.footer-line{
  height:1px;
  background:var(--fg);
  opacity:0.3;
  margin-bottom:20px;
}
footer p{
  font-size:10px;
  letter-spacing:3px;
  opacity:0.5;
}

@media (max-width:768px){
  .hero, .feed-section, footer{ padding-left:50px; padding-right:50px;}
  .side-text{ font-size:8px; }
}